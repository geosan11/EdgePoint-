# --- Node / Expo / React Native (mobile/) ---
node_modules/
.expo/
.expo-shared/
dist/
web-build/
*.jks
*.p12
*.key
*.mobileprovision
*.orig.*
npm-debug.*
yarn-debug.*
yarn-error.*
*.tsbuildinfo
.metro-health-check*

# --- Python (backend/) ---
venv/
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
*.egg-info/

# --- ML pipeline artifacts & scraped personal betting data (backend/) ---
# Never commit: trained models are unvalidated/synthetic until proven, and
# scraped bet history is real personal financial data.
backend/data/
backend/models/
*.joblib

# --- Env / secrets (all sub-projects) ---
.env
.env.local
.env.*.local
!.env.example

# --- Supabase CLI local state ---
supabase/.branches
supabase/.temp

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/
